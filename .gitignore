.agents
.amp/portals/
.claude/plans/**
!.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/**
.claude/*.lock
.codex
.DS_Store
.gitlens/workflow.state.json
.work/
.stylelintcache
.superpowers
.tasks
**/.vscode-clean/**
**/.vscode-test/**
**/.vscode-test-web/**
**/test-results/**
dist
out
node_modules
images/settings
custom-elements.json
gitlens-*.vsix
# `npm pack` output. `packages/core/.gitignore` already covers the package dir, but a tarball moved to the
# repo root for a hand-off build would otherwise be tracked (each is ~2 MB and permanent in history).
*.tgz
product.json
tsconfig*.tsbuildinfo
*.code-workspace
localdev*.instructions.md
*.log

# Build codegen skip-cache (input fingerprints for Docs/contributions/CEM generators)
.codegen-cache/
