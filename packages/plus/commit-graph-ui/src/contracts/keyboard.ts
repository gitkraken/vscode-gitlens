/** Closed scope vocabulary compiled into the graph's key dispatcher at startup. */
export type GraphKeymapScope =
	| 'webview'
	| 'webviewGlobal'
	| 'rows'
	| 'rowControl'
	| 'pillMenu'
	| 'sidebarFilter'
	| 'tree';
