{
	"name": "@gitlens/integrations-consumer-fixture",
	"private": true,
	"version": "0.0.0",
	"description": "Proof-of-extraction: imports only the public @gitkraken/core-gitlens package surface.",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json --noEmit",
		"test": "tsc -p tsconfig.json --noEmit && node scripts/run.mjs"
	},
	"dependencies": {
		"@gitkraken/core-gitlens": "workspace:*"
	},
	"devDependencies": {
		"esbuild": "catalog:",
		"typescript": "catalog:"
	}
}
