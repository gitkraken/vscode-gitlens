{
	"name": "e2e-es-language-pack",
	"displayName": "E2E Spanish Language Pack (test fixture)",
	"description": "Minimal offline stand-in for Microsoft's Spanish language pack, used only to make VS Code accept `--locale=es` in E2E tests. Its own translations are empty (VS Code's core UI stays English) — this only needs to exist so `vscode.env.language` resolves to `es`, which is all GitLens' own l10n catalog selection depends on.",
	"publisher": "gitlens",
	"version": "1.0.0",
	"engines": {
		"vscode": "*"
	},
	"contributes": {
		"localizations": [
			{
				"languageId": "es",
				"languageName": "Spanish",
				"localizedLanguageName": "español",
				"translations": [
					{
						"id": "vscode",
						"path": "./translations/main.i18n.json"
					}
				]
			}
		]
	}
}
